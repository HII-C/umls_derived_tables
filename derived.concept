# POPULATE DERIVED.TERM WITH STRS FROM DERIVED.CONCEPT
SELECT * FROM derived.term LIMIT 100;
INSERT INTO derived.term (str)
  SELECT DISTINCT str
  FROM umls.MRCONSO
  WHERE length(STR) <= 255 AND LAT = 'ENG';
SELECT * from umls.MRCONSO LIMIT 100;
